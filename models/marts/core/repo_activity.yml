version: 2

models:
  - name: repo_activity
    description: >
     ddd

    tests:
      - unique:
          column_name: "repos_id || events_id || commits_hash" 

      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - repos_id
            - events_id
          config:
            where: events_type != 'PushEvent'

    columns:
      - name: repos_id
        description: >
         ddd
        tests:
          - not_null

      - name: repos_name
        description: >
         ddd
        tests:
          - not_null

      - name: events_id
        description: >
         ddd
        tests:
          - not_null

      - name: events_type
        description: >
         ddd
        tests:
          - not_null

      - name: is_pullrequest
        description: >
         ddd
        tests:
          - not_null

      - name: is_commit
        description: >
         ddd
        tests:
          - not_null

      - name: is_watch
        description: >
         ddd
        tests:
          - not_null

      - name: commits_message
        description: >
         ddd

      - name: commits_hash
        description: >
         ddd